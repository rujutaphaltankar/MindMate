server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — client-side routing (React Router) needs every
    # unmatched path to resolve to index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
